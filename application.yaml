server:
  port: 8080
spring:
  application:
    name: greeting-service
greeting-service:
  greeting: "Hello!"
  default-value: "World"
#Actuator
management:
      endpoints:
        web:
          exposure:
            include: '*'
      endpoint:
        health:
          show-details: ALWAYS
      #Expondo todos os endpoins
