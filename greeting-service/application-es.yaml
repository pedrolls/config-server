server:
  port: 8080
spring:
  application:
    name: greeting-service
greeting-service:
  greeting: "Hola!"
  default-value: "Mundo"
#Actuator
management:
      endpoints:
        web:
          exposure:
            include: '*'
      endpoint:
        health:
          show-details: ALWAYS
      #Expondo todos os endpoins
